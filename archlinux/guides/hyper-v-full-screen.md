# HyperV Full Screen

Turn off the virtual machine, and run this command in PowerShell:

```bash
Set-VMVideo -VMName "archlinux" -HorizontalResolution 2560 -VerticalResolution 1600 -ResolutionType Single
```
